from django.apps import AppConfig


class PressOfficeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.press_office'
    verbose_name = "Выступления руководства"
